## Javascript DSL Example

An example of an external DSL written in Javascript. This requires running with iojs, or Node 0.12 with the `--harmony` flag set.

### Running the example

First, clone the repo to your machine. Then install NPM packages.

    npm install

Then you can run the sample tests.

    node run.js sample.txt
